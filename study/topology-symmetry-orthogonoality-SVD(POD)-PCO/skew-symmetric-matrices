Using principal component analysis for orthogonal decomposition ( POD ) or SVD. 

Can we retrieve the absolute index of the eigenvalue when  rescaling to try to reach reflection of the space thus finding the clustering at anytime, during neural … ? which process when? What will happen here that we can achieve ortagonality? 
 V × V → K with satisfied property AtSA = S, where S is a fixed real invertible symmetric or skew- symmetric matrix. 
Decompose value in singular representation into a pseud-euclidean space, thus 

Axioms
